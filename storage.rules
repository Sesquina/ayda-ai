rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Require authenticated users for read/write in this example
      allow read, write: if request.auth != null;
    }
  }
}
